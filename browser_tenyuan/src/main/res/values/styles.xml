<resources>

    <!-- Base application theme. Theme.AppCompat.Light.NoActionBar  parent="Theme.AppCompat.Light.NoActionBar"-->
    <style name="style_BaseAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/color_9</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:windowAnimationStyle">@style/style_activity_toggle_anim</item>-->
    </style>

    <!-- 自定义仿IOS的AlertDialog的样式 -->
    <style name="AlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    
    <!--activity之间的跳转动画-->
    <!--<style name="style_activity_toggle_anim">
        <item name="android:activityOpenEnterAnimation">@anim/anim_activity_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/anim_slide_left_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/anim_slide_left_in</item>
        <item name="android:activityCloseExitAnimation">@anim/anim_activity_exit</item>
    </style>-->
    <!--启动白屏问题-->
    <style name="theme.Splash" parent="style_BaseAppTheme">
        <!--<item name="android:windowBackground">@mipmap/img_start_page</item>-->
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="style_AppTheme" parent="style_BaseAppTheme"></style>

</resources>
